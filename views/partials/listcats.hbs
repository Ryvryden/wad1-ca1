<section class="ui left aligned middle aligned segment">
    {{> catHeader}}

    {{#unless search}}
        {{#unless felines}}
            <h3 class="ui message ">
                Start adding your Cats!
            </h3>
        {{/unless}}
    {{/unless}}

    <div class="ui centered cards">
    
    {{#each felines}}
      {{> catCard}}
    {{/each}}
    </div>

</section>